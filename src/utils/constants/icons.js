import { Activity, Ambulance, Cross } from "lucide-react";
import theme from "../theme";

const icons = [
    {
        icon: Activity,
        color: theme.colors.primary,
    },
    {
        icon: Cross,
        color: theme.colors.secondary,
    },
    {
        icon: Ambulance,
        color: theme.colors.danger,
    },
];

export default icons;